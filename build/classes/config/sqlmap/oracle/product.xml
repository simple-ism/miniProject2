<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="product.ProductDAO">
	<select id="selectLowPrice">
		select p_no as pNo, p_price as pPrice, p_name as pName, p_detail as pDetail, p_reg_date as pRegDate, p_ori as pOri, p_real as pReal, p_path as pPath
		from product 
		where 
	</select>
</mapper>











